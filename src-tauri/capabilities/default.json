{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main window to use Tauri core IPC APIs, including event listen/unlisten.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
